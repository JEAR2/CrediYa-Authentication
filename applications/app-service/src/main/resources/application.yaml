server:
  port: 8080
spring:
  application:
    name: "Authentication"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

routes:
  paths:
    users: "/api/v1/users"
    emailExists: "/api/v1/users/exists/{email}"
    login: "/api/v1/users/login"

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "test"
    schema: "sm"
    username: "sm"
    password: "1234"
    #url: "r2dbc:mysql://localhost:3306/users"
    #username: root
    #password: 1234
    #sql:
    #  init:
    #    mode: always
    #flyway:
    #  enabled: false

security:
  jwt:
    secret: ${JWT_SECRET:3Y8vRj2fA9kQm7XsZ5bW1tGeLhKdN0qCp4yUoFnJxVrTiHaM6zEcSbDgPwXrQvTy}
    expiration: 3600000
